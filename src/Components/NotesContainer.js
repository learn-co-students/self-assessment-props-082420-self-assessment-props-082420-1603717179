import React from 'react'

function NotesContainer() {

    apiResponse = () => {
        return ["First Note", "Second Note", "Third Note", "Fourth Note"]
    }

    return (
        <ul>
            {/* render notes here */}
        </ul>
    )
}